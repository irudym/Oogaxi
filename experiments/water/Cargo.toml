[package]
name = "water"
version = "0.1.0"
edition = "2024"

[dependencies]
oogaxi = { path = "../.."}
bevy.workspace = true
rand.workspace = true
bevy-inspector-egui = { version = "0.37", optional = true }


# Dev only: 'cargo run --features dev' turns on dynamic linking
[features]
dev = [
    "bevy/dynamic_linking",
    "bevy/file_watcher",     # shader hot-reload, dev only
    "bevy/bevy_dev_tools",
    "dep:bevy-inspector-egui",
]
